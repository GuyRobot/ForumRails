<%= simple_form_for(@channel) do |f| %>
  <%= f.error_notification %>

  <div class="field">
    <div class="control">
    <%= f.input :channel, input_html: {class: "input"},
    label_html: {class: "label"} %>
    </div>
  </div>

  <%= f.submit, class: "button is-info" %>

<% end %>